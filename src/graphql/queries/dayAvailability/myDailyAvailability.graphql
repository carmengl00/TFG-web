query myDailyAvailability(
    $input: MonthInput!,
){
  myDailyAvailability(
    input: $input
  ){
    day
    availabilities{
      id
      resource{
        name
      }
      startTime
      endTime
    }
  }
}