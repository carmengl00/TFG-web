query resource(
    $id: UUID!
){
  resource(
    id: $id
  ){
    user{
      email
      firstName
      lastName
    }
    id
    name
    description
    availableTime
    startDate
    endDate
    location
  }
}